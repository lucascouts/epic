#!/usr/bin/env bash
# Thin wrapper over scripts/cross-reference.sh exposed on PATH while the
# Epic plugin is active. Usage is identical to the underlying script.
exec bash "$(dirname "$0")/../scripts/cross-reference.sh" "$@"
